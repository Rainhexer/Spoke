# Rust / Tauri
src-tauri/target
src-tauri/gen

# Models (downloaded at runtime, large)
src-tauri/models/*.bin

# Build artifacts
Spoke-*.AppImage

# Local config / secrets
*.local
.env
.DS_Store

# Local tooling notes (not part of the release)
CLAUDE.md

# Local release notes (not part of the repo)
RELEASE_NOTES.md
